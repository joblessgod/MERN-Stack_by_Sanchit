/* 
const sum = (...rest) => {
    let result = 0;
    console.log("Rest:", rest);

    for (let count = 0; count < rest.length; count++) {
        // console.log(result + count);
        let number = rest[count];
        result = result + number;
    }

    return result;
}
console.log("sum:",sum(1, 2, 3, 4, 5, 6));
*/



/* 
function doubleTheArray() {
    let result = []
    let arr = [1, 2, 3]
    for (let index = 0; index < arr.length; index++) {
        result.push(index * 2)
    }
    return result
}
console.log(doubleTheArray());

// let newArray = doubleTheArray()
// console.log(newArray);

const doubleTheArray2 = () => {
    let result = []
}  
 */

/* 

let todos = [
    {
        title: "variable",
        status: "completed",
        createAt: "2024-01-03"
    },
    {
        title: "array",
        status: "completed",
        createAt: "2024-01-05"
    },
    {
        title: "object",
        status: "completed",
        createAt: "2024-01-07"
    },
    {
        title: "loop",
        status: "pending",
        createAt: "2024-01-09"
    },
    {
        title: "function",
        status: "pending",
        createAt: "2024-01-10"
    },
];

let completedTodos = []
let pendingTodos = []

for (let index = 0; index < todos.length; index++) {
    const element = todos[index];
    
    
}console.log(completedTodos); // [ make list of completed]
console.log(pendingTodos); // [ make list of pending]

 */